import { ConvertirStockDirective } from './convertir-stock.directive';

describe('ConvertirStockDirective', () => {
  it('should create an instance', () => {
    const directive = new ConvertirStockDirective();
    expect(directive).toBeTruthy();
  });
});
